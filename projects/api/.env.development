# Development Environment Configuration

# API Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_CONNECTION_STRING=localhost:27017
DB_NAME=pcb_netlist_dev

# JWT Secret (use a secure secret in production)
JWT_SECRET=your-development-secret-key-here

# Cookie Configuration (handled automatically by the config)
# For localhost development:
# - Cookie domain: localhost
# - Secure: false (HTTP allowed)
# - CORS origins: http://localhost:5174, http://127.0.0.1:5174
